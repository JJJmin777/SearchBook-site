<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <!-- Brand -->
        <a class="navbar-brand" href="/">Search</a>

        <!-- Toggler Button -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Collapsible Navbar -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <!-- Left Links -->
            <div class="navbar-nav">
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link" href="/mybooks">My Books</a>
                <!-- Uncomment if needed -->
                <!-- <a class="nav-link" href="/about">About</a> -->
            </div>

            <!-- Right Links -->
            <div class="navbar-nav ms-auto">
                <!-- Search Form -->
                <form action="/books/results" method="POST" class="d-flex me-3">
                    <div class="input-group">
                        <!-- Search Input -->
                        <input type="text" placeholder="Search book" id="searchQuery" name="bookTitle" class="form-control">
                        <!-- Search Icon as Button -->
                        <button type="submit" class="btn btn-outline-secondary">
                            <i class="fas fa-search"></i> <!-- Font Awesome Search Icon -->
                        </button>
                    </div>
                </form>

                <% if (!currentUser) { %>
                    <a class="nav-link" href="/login">Login</a>
                    <a class="nav-link" href="/register">Register</a>
                    <% } else { %>
                        <a class="nav-link" href="/logout">Logout</a>
                        <% } %>
            </div>
        </div>
    </div>
</nav>